## [2.0.0] - 2026-07-08

### ✨ Added
- Company global filter implementation — automatically applies company-level filtering to `getdoc` and `search_link` across all doctypes
- Company switcher in the ERPNext navbar for quick company switching
- Frappe v16 compatibility

### 🔧 Changed
- Refactored error logging — removed redundant exception arguments from `log_error` calls
- Simplified logging by removing f-strings from `frappe.log_error` calls
- Updated CI workflow for version-16 branch
- Bumped `requires-python` to `>=3.14` for Frappe v16 compatibility
